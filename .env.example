# Server-side only. Never use VITE_ for secrets. Set one of these keys.
GEMINI_API_KEY=
# GOOGLE_AI_STUDIO_API_KEY=
# Override if your Gemini project uses a different image + Search capable model.
GEMINI_MODEL=gemini-3.8-flash
PORT=3000
